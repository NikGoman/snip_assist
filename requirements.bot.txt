# requirements.bot.txt
# Зависимости для сервиса Telegram-бота (app/)

# Telegram бот фреймворк
aiogram>=3.0.0

# Работа с асинхронной базой данных
sqlalchemy>=2.0.0
aiosqlite>=0.17.0 # Для SQLite, если используется. Заменить на psycopg2-binary и т.д. при переходе к другим БД.
# Пример для PostgreSQL:
# psycopg2-binary>=2.9.0

# python-dotenv для загрузки переменных окружения из .env файлов
python-dotenv>=1.0.0

# Асинхронный HTTP клиент для взаимодействия с API-сервисом
httpx>=0.25.0

# Pydantic для валидации данных (может быть опционально, если используются аннотации типов в handlers)
# pydantic>=2.5.0 # Обычно не требуется напрямую для бота, если не используется в сервисах для валидации API-ответов

# (Опционально) Логирование (обычно встроено, но можно добавить для расширенных сценариев)
# structlog>=24.0.0

# (Опционально) Для удобства разработки
# black>=23.0.0
# isort>=5.0.0
# mypy>=1.0.0

# --- Удалены зависимости, связанные с RAG, которые теперь находятся в rag/ ---
# llama-index>=0.9.0
# chromadb>=0.4.0
# transformers>=4.35.0
# torch>=2.1.0
# llama-cpp-python>=0.2.0
# sentence-transformers>=2.2.0
