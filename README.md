# Проверенный помощник по СНиП и ГОСТ

AI-ассистент для прорабов и технических директоров в Telegram. Позволяет быстро находить и интерпретировать нормативные документы (ГОСТ/СНИП) с подтверждёнными ссылками.

## Технические особенности

- **RAG-архитектура** на базе `LlamaIndex` и `Chroma`
- **Локальная LLM** (`Phi-3-mini`) для отказоустойчивости
- **Ограничение запросов** (3/день для бесплатных пользователей)
- **Проверяемые источники** в ответах
- **Безопасность и надёжность** по умолчанию
- **Готов к продакшену** и интеграции в CI/CD

## Архитектура репозитория

construction_rag_bot/
├── app/                    # Основной код приложения
│   ├── __init__.py
│   ├── bot/                # Логика Telegram-бота
│   │   ├── __init__.py
│   │   ├── handlers/       # Обработчики команд и сообщений
│   │   │   ├── __init__.py
│   │   │   ├── start.py    # Команда /start
│   │   │   ├── query.py    # Обработка текстовых запросов
│   │   │   └── limits.py   # Обработка лимитов (опционально)
│   │   ├── keyboards/      # Клавиатуры (опционально)
│   │   ├── filters/        # Фильтры сообщений (опционально)
│   │   └── middleware/     # Промежуточная логика (лимиты, логирование)
│   ├── core/               # Ядро приложения
│   │   ├── __init__.py
│   │   ├── config.py       # Настройки приложения
│   │   ├── database.py     # Модель и сессия базы данных
│   │   └── rag.py          # Логика RAG и интеграция с LLM
│   └── services/           # Бизнес-логика
│       ├── __init__.py
│       └── query_service.py # Основной сервис обработки запросов
├── data/                   # Данные (векторные эмбеддинги)
│   └── embeddings/
├── tests/                  # Тесты
│   ├── __init__.py
│   ├── test_bot.py         # Тесты бота
│   ├── test_rag.py         # Тесты RAG-логики
│   └── conftest.py         # Фикстуры для тестов
├── .env.example            # Пример файла конфигурации
├── .gitlab-ci.yml          # CI/CD конфигурация
├── Dockerfile              # Файл для контейнеризации
├── requirements.txt        # Зависимости Python
├── pyproject.toml          # Метаданные проекта (опционально)
└── README.md               # Этот файл

## Запуск

1. Установите зависимости: `pip install -r requirements.txt`
2. Создайте `.env` и укажите `TELEGRAM_BOT_TOKEN`
3. Запустите: `python -m app.main`
